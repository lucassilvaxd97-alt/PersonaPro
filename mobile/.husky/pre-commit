#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# run lint-staged (fallback to local binary if npx is not available)
if command -v npx >/dev/null 2>&1; then
  npx --no -- lint-staged
else
  ./node_modules/.bin/lint-staged
fi
